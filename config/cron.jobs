SHELL=/bin/bash
40 * * * 1 XDG_RUNTIME_DIR=/run/user/$UID systemctl --user start switcharoo_flair
35 2 * * * cd ~/switcharoo && ~/venv/bin/python check_up.py -s last -l 20 --no-relink --unmute-delete
